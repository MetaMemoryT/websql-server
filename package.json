{
  "name": "ct-server",
  "version": "1.0.0",
  "description": "server for running plugins in browser",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "primus": "^2.4.10",
    "sqlite3": "^3.0.4"
  }
}
